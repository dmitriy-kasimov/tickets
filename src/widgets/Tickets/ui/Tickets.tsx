import { FC } from 'react'
import { TicketsFeedFallback } from '@/widgets/Tickets/ui/TicketsFeedFallback.tsx'

export const Tickets: FC = () => {
    return <TicketsFeedFallback />
    // return <TicketsFeed tickets={[]} />
}
